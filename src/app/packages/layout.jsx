export const metadata = {
  title: "Exclusive Travel Packages | TripLinkers",
  description: "Browse curated travel packages for top destinations. Find the perfect itinerary for your budget and style.",
};

export default function PackagesLayout({ children }) {
  return <>{children}</>;
}
